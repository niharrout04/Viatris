<?xml version="1.0" encoding="UTF-8" ?>
<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core" xmlns:l="sap.ui.layout">
    <VBox>
        <!-- <Panel expandable="true" expanded="true" headerText="PURCHASING DETAILS 1" class="sapUiMediumMarginTop vendorRegistrationFormPanel">
            <VBox>
                <l:Grid defaultSpan="XL3 L3 M6 S12">
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>st1}" class="sigLabel" required="true" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>st1}"
                            value="{oSupplierDetailModel>/vendorAddress/street}"
                            maxLength="60"
                            valueState="{oMandatoryModel>/vendorAddress/streetState}"
                            valueStateText="{i18n>ManFields}"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            liveChange="onAddingmandvalue"
                        >
                            <customData>
                                <core:CustomData key="target" value="street" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>st2}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>st2}"
                            value="{oSupplierDetailModel>/vendorAddress/street2}"
                            maxLength="40"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="street2" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>st3}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>st3}"
                            value="{oSupplierDetailModel>/vendorAddress/street3}"
                            maxLength="40"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="street3" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>st4}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>st4}"
                            value="{oSupplierDetailModel>/vendorAddress/street4}"
                            maxLength="40"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="street4" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>st5}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>st5}"
                            value="{oSupplierDetailModel>/vendorAddress/street5}"
                            maxLength="40"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="street5" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>City}" class="sigLabel" required="true" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>City}"
                            value="{oSupplierDetailModel>/vendorAddress/city}"
                            maxLength="40"
                            valueState="{oMandatoryModel>/vendorAddress/cityState}"
                            valueStateText="{i18n>ManFields}"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            liveChange="onAddingmandvalue"
                        >
                            <customData>
                                <core:CustomData key="target" value="city" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Pincode}" class="sigLabel" required="true" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>Pincode}"
                            value="{oSupplierDetailModel>/vendorAddress/cityPostalCode}"
                            liveChange="validatePostalCode"
                            valueState="{oMandatoryModel>/vendorAddress/cityPostalCodeState}"
                            valueStateText="{i18n>ManFields}"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="cityPostalCode" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Country}" class="sigLabel" required="true" />
                        <ComboBox
                            class="sigComboBox"
                            width="100%"
                            placeholder="Select {i18n>Country}"
                            items="{oDropdownModel>/country}"
                            change="onCountrySelect"
                            selectedKey="{oSupplierDetailModel>/vendorAddress/countryKey}"
                            valueState="{oMandatoryModel>/vendorAddress/countryKeyState}"
                            valueStateText="{i18n>ManFields}"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            showSecondaryValues="true"
                            selectionChange="onAddingmandvalue"
                        >
                            <core:ListItem key="{oDropdownModel>countryKey}" text="{oDropdownModel>countryKey}" additionalText="{oDropdownModel>countryName}" />
                        </ComboBox>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>CompanyPhone}" class="sigLabel" required="true" />
                        <HBox renderType="Bare">
                            <ComboBox
                                class="sigComboBox"
                                width="50%"
                                items="{oDropdownModel>/phoneCountryCode}"
                                showSecondaryValues="true"
                                change="onChangeCountryCode"
                                selectedKey="{oSupplierDetailModel>/countryCode}"
                                valueState="{oMandatoryModel>/phoneCountrycodeState}"
                                valueStateText="{i18n>ManFields}"
                                selectionChange="onAddingmandvalue"
                                enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            >
                                <core:ListItem key="{oDropdownModel>countryCode}" text="{oDropdownModel>countryCode}" additionalText="{oDropdownModel>countryKey}" />
                            </ComboBox>
                            <Input
                                class="sigInputText"
                                placeholder="Enter {i18n>CompanyPhone}"
                                value="{oSupplierDetailModel>/mobileNumber}"
                                valueState="{oMandatoryModel>/mobileNumberState}"
                                valueStateText="{i18n>ManFields}"
                                liveChange="onPhoneNumberChange"
                                enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            />
                        </HBox>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Fax}" class="sigLabel" />
                        <Input class="sigInputText" placeholder="Enter {i18n>Fax}" value="{oSupplierDetailModel>/fax}" maxLength="40" enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}" />
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Website}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>Website}"
                            value="{oSupplierDetailModel>/vendorAddress/website}"
                            maxLength="2048"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="website" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Region}" class="sigLabel" />
                        <ComboBox
                            class="sigComboBox"
                            width="100%"
                            placeholder="Select {i18n>Region}"
                            items="{oDropdownModel>/region}"
                            selectedKey="{oSupplierDetailModel>/vendorAddress/region}"
                            change="oRegionChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <core:Item key="{oDropdownModel>regionKey}" text="{oDropdownModel>regionName}" />
                        </ComboBox>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Fax}" class="sigLabel" />
                        <Input class="sigInputText" placeholder="Enter {i18n>Fax}" value="{oSupplierDetailModel>/fax}" maxLength="40" enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}" />
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Website}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>Website}"
                            value="{oSupplierDetailModel>/vendorAddress/website}"
                            maxLength="2048"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="website" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Region}" class="sigLabel" />
                        <ComboBox
                            class="sigComboBox"
                            width="100%"
                            placeholder="Select {i18n>Region}"
                            items="{oDropdownModel>/region}"
                            selectedKey="{oSupplierDetailModel>/vendorAddress/region}"
                            change="oRegionChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <core:Item key="{oDropdownModel>regionKey}" text="{oDropdownModel>regionName}" />
                        </ComboBox>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>st4}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>st4}"
                            value="{oSupplierDetailModel>/vendorAddress/street4}"
                            maxLength="40"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="street4" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>st5}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>st5}"
                            value="{oSupplierDetailModel>/vendorAddress/street5}"
                            maxLength="40"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="street5" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>City}" class="sigLabel" required="true" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>City}"
                            value="{oSupplierDetailModel>/vendorAddress/city}"
                            maxLength="40"
                            valueState="{oMandatoryModel>/vendorAddress/cityState}"
                            valueStateText="{i18n>ManFields}"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            liveChange="onAddingmandvalue"
                        >
                            <customData>
                                <core:CustomData key="target" value="city" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Pincode}" class="sigLabel" required="true" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>Pincode}"
                            value="{oSupplierDetailModel>/vendorAddress/cityPostalCode}"
                            liveChange="validatePostalCode"
                            valueState="{oMandatoryModel>/vendorAddress/cityPostalCodeState}"
                            valueStateText="{i18n>ManFields}"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="cityPostalCode" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Country}" class="sigLabel" required="true" />
                        <ComboBox
                            class="sigComboBox"
                            width="100%"
                            placeholder="Select {i18n>Country}"
                            items="{oDropdownModel>/country}"
                            change="onCountrySelect"
                            selectedKey="{oSupplierDetailModel>/vendorAddress/countryKey}"
                            valueState="{oMandatoryModel>/vendorAddress/countryKeyState}"
                            valueStateText="{i18n>ManFields}"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            showSecondaryValues="true"
                            selectionChange="onAddingmandvalue"
                        >
                            <core:ListItem key="{oDropdownModel>countryKey}" text="{oDropdownModel>countryKey}" additionalText="{oDropdownModel>countryName}" />
                        </ComboBox>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>CompanyPhone}" class="sigLabel" required="true" />
                        <HBox renderType="Bare">
                            <ComboBox
                                class="sigComboBox"
                                width="50%"
                                items="{oDropdownModel>/phoneCountryCode}"
                                showSecondaryValues="true"
                                change="onChangeCountryCode"
                                selectedKey="{oSupplierDetailModel>/countryCode}"
                                valueState="{oMandatoryModel>/phoneCountrycodeState}"
                                valueStateText="{i18n>ManFields}"
                                selectionChange="onAddingmandvalue"
                                enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            >
                                <core:ListItem key="{oDropdownModel>countryCode}" text="{oDropdownModel>countryCode}" additionalText="{oDropdownModel>countryKey}" />
                            </ComboBox>
                            <Input
                                class="sigInputText"
                                placeholder="Enter {i18n>CompanyPhone}"
                                value="{oSupplierDetailModel>/mobileNumber}"
                                valueState="{oMandatoryModel>/mobileNumberState}"
                                valueStateText="{i18n>ManFields}"
                                liveChange="onPhoneNumberChange"
                                enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            />
                        </HBox>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>Fax}" class="sigLabel" />
                        <Input class="sigInputText" placeholder="Enter {i18n>Fax}" value="{oSupplierDetailModel>/fax}" maxLength="40" enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}" />
                    </VBox>
                
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>st4}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>st4}"
                            value="{oSupplierDetailModel>/vendorAddress/street4}"
                            maxLength="40"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="street4" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>st5}" class="sigLabel" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>st5}"
                            value="{oSupplierDetailModel>/vendorAddress/street5}"
                            maxLength="40"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                        >
                            <customData>
                                <core:CustomData key="target" value="street5" />
                            </customData>
                        </Input>
                    </VBox>
                    <VBox class="sapUiSmallMarginTop">
                        <Label text="{i18n>City}" class="sigLabel" required="true" />
                        <Input
                            class="sigInputText"
                            placeholder="Enter {i18n>City}"
                            value="{oSupplierDetailModel>/vendorAddress/city}"
                            maxLength="40"
                            valueState="{oMandatoryModel>/vendorAddress/cityState}"
                            valueStateText="{i18n>ManFields}"
                            change="oAddressChange"
                            enabled="{parts: ['oSupplierDetailModel>/notExtended','oSupplierDetailModel>/isChanged'], formatter:'.formatter.setInputenbled'}"
                            liveChange="onAddingmandvalue"
                        >
                            <customData>
                                <core:CustomData key="target" value="city" />
                            </customData>
                        </Input>
                    </VBox>
                    </l:Grid>
            </VBox>
        </Panel> -->
    </VBox>
</core:FragmentDefinition>